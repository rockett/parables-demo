1. Install the parables library

    cd into a temp directory

    git clone git://github.com/mlurz71/parables.git 

    cp -R /path/to/parables/My /path/to/parables-demo/library/

2. Generate models from Yaml, generate database and load fixtures.

    cd /path/to/parables-demo/script/doctrine.sh build-all-reload

3. Setup a virtual host using /path/to/parables-demo/public as docroot and
update your hosts file

4. Browse the virtual host

